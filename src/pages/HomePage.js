export default function HomePage() {
    // in useEffect, fetch posts from: https://race-rest-default-rtdb.firebaseio.com/posts.json

    return (
        <section className="page">
            <h1>Posts Page</h1>
        </section>
    );
}
